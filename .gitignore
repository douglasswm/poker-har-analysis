# Raw capture contains LIVE auth tokens — never commit it.
poker.har
*.raw.har

# Use poker.redacted.har as the shared test fixture instead.

# Node / build
node_modules/
dist/
build/
*.log
.DS_Store

# Generated test/parity runner bundles (esbuild output)
engine/test/.run.bundle.cjs
engine/test/.parity.bundle.cjs

# Local agent / editor config and Python caches
.claude/
__pycache__/
*.pyc
