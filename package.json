{
  "name": "tengan-poker-hud",
  "version": "0.1.0",
  "description": "Passive Stake Poker websocket viewer + from-scratch Discounted-CFR GTO advisor (TypeScript). Load the folder unpacked in Chrome.",
  "type": "module",
  "scripts": {
    "start": "npm run solver:start",
    "dev": "node scripts/build.mjs --watch",
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs && node test/parser.test.js && node test/solver-env.test.js && node test/solver-server.test.js && node test/bridge-native-fallback.test.js && node scripts/parity.mjs",
    "parity": "node scripts/parity.mjs",
    "parity:gen": "node scripts/parity.mjs --gen",
    "solver": "node scripts/start-solver.mjs",
    "solver:start": "node scripts/start-solver.mjs",
    "solver:server": "node solver-server/server.js",
    "solver:check": "node scripts/check-solver-server.mjs",
    "typecheck": "tsc -p engine/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.21.5",
    "typescript": "^5.4.0"
  }
}
