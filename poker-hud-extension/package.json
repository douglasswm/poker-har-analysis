{
  "name": "tengan-poker-hud",
  "version": "0.1.0",
  "description": "Passive Stake Poker websocket viewer + from-scratch Discounted-CFR GTO advisor (TypeScript). Load the folder unpacked in Chrome.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --watch",
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs && node test/parser.test.js",
    "typecheck": "tsc -p engine/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.21.5",
    "typescript": "^5.4.0"
  }
}
